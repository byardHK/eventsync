import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#04227a',
      dark: '#859cf980',
      contrastText: '#FFFFFF',
    },
  },
});

export default theme;